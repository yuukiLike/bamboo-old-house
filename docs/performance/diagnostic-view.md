# 运行时实时监测、加载进度与完整报告

用户选择：只在诊断模式显示，不改变普通访客的加载界面。

## 打开实时面板

生产预览服务启动后，在 Chrome Canary 打开：

```text
http://127.0.0.1:4175/?perf=1&perfUI=1
```

`perf=1` 开启阶段记录；`perfUI=1` 额外显示面板。只使用 `perf=1` 的批量采集不显示面板。面板动态导入，复用项目已有的 [Base UI Progress](https://base-ui.com/react/components/progress)，不再安装另一个进度条库。

面板默认打开 **实时运行**，可切换回 **加载时间线**。折叠面板或切换页签不会停止实时采集；显式暂停、页面隐藏或卸载面板才会停止。

## 实时运行

实时页提供：

- **近 5 秒 RAF 回调率、p95 与最大间隔**：统计最近 5 秒内结束的完整前台 RAF 间隔。跨窗口起点的长间隔仍保留完整时长，因此样本间隔合计可能超过 5 秒；不会把一次 6 秒卡顿裁成 5 秒或在恢复时漏掉。
- **浏览器回调频率与实时状态**：实时指标用「浏览器回调频率」和「近 5 秒 · RAF」展示。主数字及折叠标题使用同一颜色和状态标签：绿色「合适」、黄色「中等」、红色「卡顿」；黄色与红色显示具体原因。判定只用于展示，不改变原有帧统计、导出指标和卡顿采集阈值。
- **帧率怎么看**：默认展开、可折叠的新手参考面板，统一解释 Hz、FPS、p95 和实时颜色。Hz 是每秒发生的次数，60 Hz 约等于每秒 60 次 RAF 回调；RAF 是浏览器安排页面准备下一帧的回调，这个数字不是屏幕实际呈现的 FPS，也不是检测到的屏幕刷新率。帧率参考包括常见的 60 FPS 流畅目标（约 16.7 ms / 帧）、30 FPS（约 33.3 ms / 帧）、更低帧率的体验差别，以及 120 Hz 屏幕对应的 120 FPS 参考。
- **近 30 秒图**：每 200 ms 桶保留最大间隔，按结束时刻定位。50 / 100 ms 为诊断分级，颜色对应慢间隔与更长停顿；柱高最多显示到 500 ms，超长值在数字、详情和悬停提示中完整保留。
- **最近卡顿**：导航后的起点、完整时长、附近场景 / 地点 / 声音 / 天气状态、期间真实控件操作和重叠业务阶段。另列间隔开始前 2 秒内最近一次操作及相隔时长，便于追踪异步转场；它不是因果判定。默认最近 6 条，可展开最多 120 条保留记录。
- **渲染器快照**：draw calls、triangles、纹理 / 几何体数量、绘图尺寸、pixel ratio 和图形后端。来自应用已有诊断，每 15 个应用帧更新；数量不是显存字节。
- **主线程长任务**：支持 Long Tasks 的浏览器显示近 5 秒内结束的任务数量与最长时长；不支持时明确说明，不填零。

实时颜色以固定的 **60 Hz** 为参考，按近 5 秒内的回调频率、p95 和最大间隔取最严重的一档，不自动检测屏幕刷新率：

| 颜色与状态 | 条件 |
| --- | --- |
| 绿色 · 合适 | 显示频率 ≥55.0 Hz，且 p95 ≤25 ms，且最大间隔 <50 ms |
| 黄色 · 中等 | 不满足绿色条件，也未达到红色条件 |
| 红色 · 卡顿 | 显示频率 <30.0 Hz，或 p95 ≥50 ms，或最大间隔 ≥100 ms |

频率按主数字显示的一位小数参与判断，p95 与最大间隔按原始值判断。灰色表示未采集、暂停、页面隐藏、异常、本次连续采集不足 5 秒或有效间隔累计不足 1 秒；启动、重开和恢复后需等满新的 5 秒，避免用旧窗口显示当前状态，也避免浏览器迟迟没有回调后仅凭少量样本判断流畅。不过本次连续段一旦记录到完整的 ≥100 ms 间隔，就会提前显示红色。主线程阻塞时页面内面板同样无法更新，恢复后才显示完整间隔及其状态。

这些颜色是本工具的体验参考，不是统一合格线；30 Hz 设备、节能模式或更高刷新率设备仍需结合实际目标理解。图表及卡顿事件原有的单次间隔黄色 ≥50 ms、红色 ≥100 ms 标记保持不变，区别于综合频率、p95 与最大间隔的实时状态。

采集异常时，标题与正文统一显示异常状态，保留的图表和场景快照标为最后一次采样；后续成功读取采样后自动恢复正常状态，不会永久停留在异常提示。

`暂停采集` 冻结读数和时间轴，`继续采集` 从新边界开始；切到后台也停止取样，恢复时不计跨隐藏边界的间隔。`清空窗口` 清除实时历史，保留加载时间线，且不会擅自解除手动暂停。`导出 JSON` 包含 `runtime` 快照、已保留卡顿和长任务，以及原有加载阶段和资源数据。

每 250 ms 刷新一次 UI，RAF 原始历史保留最近 30 秒并设置 12,000 条容量上限；卡顿与长任务分别最多 120 条，容量覆盖有计数提示。状态不是逐帧精确追踪，操作与阶段只保存有界线索：最近最多 120 次操作，每个卡顿最多 12 个重叠阶段。阶段中的 `running` 表示发现该卡顿时尚在进行，不能当作当前仍未结束。尚未结束的 RAF 间隔只有在下一次回调到来后才能确定。

采集器从诊断面板挂载后开始，不能补录此前的帧。它不改渲染循环，不测 GPU 时间或真实屏幕呈现；**RAF Hz 不是屏幕 FPS**。暂停产品动态也不等于停止 RAF。适配器的场景状态与重叠线索用于定位，不能单独证明卡顿原因。正常批量基线仍使用隐藏面板配置，避免加入实时图和 DOM 刷新的开销。

帧率参考依据：[web.dev 渲染性能](https://web.dev/articles/rendering-performance)、[MDN requestAnimationFrame](https://developer.mozilla.org/en-US/docs/Web/API/Window/requestAnimationFrame)。平均回调率接近目标，也可能夹杂短暂停顿；同时看 p95、最大间隔与具体卡顿记录。50 / 100 ms 是本工具的诊断分级，不是每帧流畅预算，未触发颜色标记不能解释为稳定达到 60 FPS。

## 加载时间线

加载页增加 HTTP 缓存汇总与逐资源标签，实时页保留紧凑概况；沿用资源观察器，继续记录懒加载的模型、音频和模块。分别显示本地复用、重新验证（推断）、网络传输与未知，以及本地复用率、含协商复用比例和可见传输体积。页面内无法读取任意资源的响应头，外部流水线会补充真实 304 与缓存标记，详见 [HTTP 缓存](./http-cache.md)。导出的资源带有 `cache` 分类，`resourceCache` 保留汇总、统计范围和淘汰数量。

加载页保留三类信息：

- **正在执行**：阶段名称、已等待时长、不确定进度条。下载、编译等无法预知最终耗时，不显示虚假的完成百分比。
- **完成的时间条**：导航、资源和业务阶段的实际起点与时长。模型下载、解析、场景构建、着色器准备、预热、首帧提交等分别保留；能够检索原始 phase 名称。
- **进入页面之后**：继续保存视图切换、首次室内管线、声音下载与解码等记录；支持导出本地 JSON。

时间统一相对本次导航的 `performance.now()` 原点。首屏控件就绪时间取 `startup.controls-ready`，它表示 React 提交后控件已启用，**不等于画布淡入结束、GPU 完成或屏幕首次呈现**。外部采集器另外检查画布淡入完成，报告中的“近似就绪”包含驱动轮询延迟。两种边界不会合并成一个看似精确的数字。

父阶段包括子阶段，多个模型可能并行加载，因此时间条不能相加为总耗时，也不能把“完成了几条记录”当作整体加载百分比。

## 主线程卡住时会发生什么

页面内的 React 面板也运行在主线程。同步场景构建、某些渲染调用阻塞时，它无法持续刷新；任务结束后会回填完整阶段耗时。面板没有为了刷新而改变加载顺序或给原业务插入 yield。

导航初期的 HTML、脚本和样式请求发生在面板加载之前，面板可在启动后从 Performance API 回填。它不能在自己的 JavaScript 尚未下载时就显示实时进度。资源缓冲和阶段记录有容量限制，缺失、截断和失败不能解释为耗时为零。

这也是完整报告仍然必需的原因：外部探针在导航前安装，记录 RAF 长间隔、长任务和业务事件；诊断 trace 则用于核对主线程调用栈。

## 打开完整报告

```sh
# 先设置 PERF_CHROME 与 PERF_DRIVER，见 scripts/perf/README.md
pnpm perf --flow views --profile desktop --mode diagnostic --iterations 1 \
  --out outputs/performance/desktop-views-diagnostic
```

打开产物中的 `report.html`：首页先看关键数字和各场景横条对比，通过导航进入首屏时间轴和重点卡顿。「停顿定位」也提供默认展开、可折叠的帧率参考面板，与实时页使用相同解释。完整原始表保留在折叠详情里。导航从 0 ms 开始，浏览器阶段、业务阶段、资源与 RAF 停顿共用同一横轴；重叠不代表因果，调用栈用于进一步归因。各按钮操作保留自己的观察窗口和状态快照。

为了评估平常体验，另用 `--mode baseline --iterations 3`。面板显示与否、trace 开关、浏览器版本、画布尺寸、声音和天气状态均会影响结果；报告核对配置和逐场景状态后才允许输出前后差值。打开实时面板的录制不能与隐藏面板的录制计算产品优化收益。

## 本次验证记录（2026-09-21）

使用 Chrome Canary 156.0.8066.0 和生产构建，复用现有采集器执行真实操作；没有新增测试用例，也没有为验证而修改场景渲染、声音或转场逻辑。

| 范围 | 操作与结果 | 本地产物 |
| --- | --- | --- |
| PC 实时运行 | 进入页面、切换竹林望月与林间的风并开启声音；读数、状态和操作线索更新。暂停后时间轴与样本冻结；暂停时清空归零；继续和折叠后持续采集；加载页与 JSON 导出可用 | `outputs/performance/canary-runtime-final/` |
| 移动端与后台边界 | 402 × 874 视口下，面板宽 360、高 590，完整位于视口内。真实切换到新标签页再返回，隐藏期间样本数保持不变，没有跨隐藏边界的 RAF 间隔 | `outputs/performance/canary-runtime-mobile/` |
| 报告展示 | 原有 baseline 和 recheck 原始采集保留，重新生成 HTML；核对首页、场景筛选、首屏时间轴与对比视图。实际从第 3 轮详情跳转后，显示第 3 轮时间轴和对应原始 JSON | `outputs/performance/canary-desktop-views-baseline/`、`outputs/performance/canary-desktop-views-recheck/` |

实时面板录制用于验证观测工具，不作为新的优化收益基线。原有正式报告的场景汇总、比较结果和状态未因展示改版而改变。上述首次运行时验证的构建归档在 `outputs/performance/build-v4-runtime/`，其中 `assets.json` 的 SHA-256 为 `b3cf17d542fcbc55b7b0915bb99d081cca7b648df8552f6de3c7272d106e94f2`。

`pnpm typecheck`、`pnpm lint`、现有 24 项测试、生产构建与报告脚本语法检查通过。采集 JSON、截图、构建与 source map 保留在被 Git 忽略的本地 `outputs/`，提交包含实现及使用文档。

[批量工具说明](../../scripts/perf/README.md) · [完整页面流程图](./page-lifecycle.md) · [实测证据与卡顿位置](./first-capture.md)
